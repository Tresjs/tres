name: Cache Monorepo
description: Cache Nx cache for monorepo workflows (pnpm store is cached by setup-node)
runs:
  using: "composite"
  steps:
    - name: Cache Nx cache
      uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
      with:
        path: .nx/cache
        key: ${{ runner.os }}-nx-cache-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-nx-cache-
